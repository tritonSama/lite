import 'package:flutter/material.dart';

class AppTheme {
  AppTheme._();

  static const Color background = Color(0xFF0C0F1D);
  static const Color primary = Color(0xFF0096C7);
  static const Color accentCyan = Color(0xFF0096C7);
  static const Color accentMagenta = Color(0xFF5300FF);
  static const Color textMain = Colors.white;
  static const Color textMuted = Colors.white70;
}
